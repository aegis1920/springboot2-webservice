# CodeDeploy의 버전.
version: 0.0
os: linux
# 이동시킬 대상이 /(root)로 전부 다 하겠다는 말.
files:
  - source:  /
    destination: /home/ec2-user/app/step2/zip/
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user

hooks:
  ApplicationStart:
    - location: deploy.sh
      timeout: 60
      runas: ec2-user